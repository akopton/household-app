"use client"

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export const ShoppingListDashboardCard = () => {
  return (
    <Card className="h-full w-full">
      <CardHeader>
        <CardTitle>Shopping</CardTitle>
      </CardHeader>
      <CardContent></CardContent>
    </Card>
  )
}
